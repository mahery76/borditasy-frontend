export interface Statistique {
    nom_produit: string;  // Name of the product
    total_profit: number;     // Quantity purchased
    total_quantity_sold: number;     // Profit made from the sales
}
